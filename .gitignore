# Local tooling / preview config
.claude/

# Archived / unsorted originals (kept on disk, excluded from repo & live site)
images/_archive/
documents/_archive/

# Source documents (Word) — only the exported PDFs are published
*.docx

# OS & editor junk
.DS_Store
Thumbs.db
desktop.ini
*.tmp
*~
*.swp
